# SYNTH-545 聚合原料分拣(中间产物)

唯一信息源:`v3-issue/synthesized/SYNTH-545-context-shared-cli.md`(下称"草稿",行号以该文件当前 643 行版本为准)。本文件是 `aggregate.md` 的分拣底稿,登记三类原料的出处、去重结论、引用分类与矛盾发现位置。**只登记,不裁决。**

## 一、筐 A:设计不变量出处对照

聚合稿的 D1–D15 与草稿章节的对应关系:

| ID | 不变量主题 | 草稿出处 |
|---|---|---|
| D1 | shared.md 并存定位与通道分类 | 设计裁决 1(38 行);RFC-1 接口假设裁决引文(81–85 行) |
| D2 | scope 闭集与 group 键 | 设计裁决 2(39 行);RFC-1 接口假设(81 行) |
| D3 | 授权无粒度、author 凭证推导 | 设计裁决 3(40 行);约束节"不新增第三类主体"(100 行) |
| D4 | required\|expected 执法与 outcome | 设计裁决 4(41 行);「必须调用」执法机制(69 行);read 无输出条件(71 行) |
| D5 | body 不透明 | entry 模型(53 行);约束节(99 行) |
| D6 | append-only 与级联 GC | entry 模型(54 行);存储与生命周期(65 行) |
| D7 | 一律经 socket、审计、穷尽分类 | entry 模型(55 行);#594 性质 2(472 行) |
| D8 | 拉取制与用法文档注入 | 读取形态(59 行) |
| D9 | 大内容无任意上限 | 存储与生命周期(64 行);已裁实现边界(94 行) |
| D10 | 查询过滤闭集与 keyset cursor | 已裁实现边界(93 行);#730 查询契约裁决(174–175 行) |
| D11 | scope 键解析有效、v2 group 拒绝 | #594 性质 6/7(476–477 行);已裁 envelope 与寻址形态(483 行) |
| D12 | 区域共享不承担后继交付 | 边界补充节(43–47 行) |
| D13 | 边界重述(无状态前提受控修订) | 边界重述节(73–77 行) |
| D14 | 代码红线 ADT | 约束节(101 行);#594 约束(492 行) |
| D15 | read boundary 归属与 envelope 演进 | RFC-5 接口假设(88 行);已裁实现边界(92 行) |

## 二、筐 B:交付标准来源与去重

来源清单(7 处):

1. RFC 关闭验证表 R1–R9(107–117 行)
2. `#730` 验收表 7 行(179–187 行)
3. `#731` 验收表 7 行(229–237 行)
4. `#732` 验收表 8 行(280–289 行)
5. `#733` 验收表 8 行(333–342 行)——**与 #732 表逐字相同**
6. `#734` 验收表 5 行(380–386 行)+ 对 R1–R9 的冻结 SHA 复核表(392–402 行,与 RFC 表同组)
7. `#594` executable contract C01–C12 / P01–P05(557–581 行)——已交付,归"已落地"证据

去重结论:

- **#733 与 #732 正文逐字重复**:问题节(261 vs 313 行)、六条性质(265–272 vs 318–325 行)、显式决策项(276 vs 329 行)、验收表(280–289 vs 333–342 行)全部一致,唯一差异是各自目标句(255 vs 308 行)。标准池只收一份;差异登记为矛盾 K3。
- **#734 复核表 = RFC R1–R9 原表**:不重复入池,以"终态复核"标记附着于全池带 R 标记的行。
- **R 行与池内标准映射**:R1→S01、R2→S14、R3→S02、R4→S08、R5→S30、R6→S31、R7→S04(写半边)+S16(读半边)、R8→S03、R9→S13。
- `#594` 契约行与池内已落地标准映射:C04→S01、C07/C11/P03→S02、C07→S03、C05→S04+S09、C10→S05+S12、C06→S06+S07、C09→S08、C01/P02→S10、C08→S11、C12→S13(一次性证据)。

## 三、筐 C:引用分类

草稿中出现的全部编号引用,逐个分类与处置:

| 草稿引用 | 分类 | 处置 |
|---|---|---|
| #738 | 跨树·入向 | 翻译为 CAP-IN-1(工具声明位) |
| #558 | 跨树·入向 | 翻译为 CAP-IN-2(树运行态 shape) |
| #698、#699、#700 | 跨树·入向 | 翻译为 CAP-IN-3(真实 par 生产调度) |
| #700、#705、#706(#733 依赖列) | 跨树·入向 | 翻译为 CAP-IN-4(trigger/validator 统一 lifecycle) |
| #544 | 跨树·出向 | CAP-OUT-1 的消费方(GUI 纯消费 read boundary) |
| #543 | 跨树·出向 | CAP-OUT-3 的消费方(hook 走 operator 读取面) |
| #546 | 已裁设计事实 | 通道边界裁决与 group 键裁决已逐字入 RFC 正文,归不变量 D1/D2 与 CAP-OUT-2;不作依赖 |
| #547 | 已裁设计事实 | required\|expected 词表、声明位归属、四轴建模已裁;其实现位即 CAP-IN-1 |
| #684、#685 | repo 流程门 | CLAUDE.md 钉定的整链路 integration / compatibility real-e2e 门;非能力依赖 |
| #406、#409、#411 | v2 已落地地基 | 凭证 / 鉴权分类 / 事件流,归"已存在能力",非依赖 |
| #396、#405、#453 | v2 设计事实与历史裁决 | 内容通道≠流转信号、流转收口 CLI、context 暂缓裁决;已融入不变量,归出处 |
| #594、PR #677、merge d381d06c | 树内已落地 | 归"已落地能力"与证据 |
| PR #655 | 历史(closed unmerged) | 非迁移源;其 socket 提前关闭回归要求已被 #594 契约吸收(S12) |
| #595–#598 | 已替代草稿(NOT_PLANNED) | 仅历史,不入聚合 |
| #78、#109 | 红线依据 | 归约束出处 |
| #534、#708 | 范围外 / 先例引用 | #534 归范围外节;#708 是文档 drift 一等偏离先例 |
| #535、#536、#538、#616、#619、#610 | 历史排序前提 | #594 落地时已满足,无剩余效力 |
| **#727** | **未识别** | 出现于 #730 Blocks(192 行),草稿零定义,登记 K5 |
| **#744** | **未识别** | 出现于 #732/#733 Blocks(294/347 行),草稿零定义,登记 K5 |

## 四、矛盾发现位置

| ID | 症状 | 草稿位置 |
|---|---|---|
| K1 | #731 目标句"必须依赖真实 par 生产调度……禁止直接 store fixture 冒充" vs 验收行"fixture 构造 `par(leaf, leaf)` 树运行态(不经调度)";#734 目标句"新增真实 parallel group communication 关闭行"暗示真实调度验证在终态综合——三处口径不一 | 205 行 vs 232 行 vs 361 行 |
| K2 | #732 目标句限"普通 scheduler run",验收表含 trigger phase 行(一视同仁),该行前提是 trigger 迁入统一 lifecycle(CAP-IN-4)——标准可验证时点晚于该 issue 其余标准 | 255 行 vs 285 行 |
| K3 | #733 无独立交付标准:正文与 #732 逐字重复,唯一差异是目标句;按正文执行则零剩余交付物 | 247–294 行 vs 298–347 行 |
| K4 | 正文明写"落地时裁"的两个显式决策项:(a) 嵌套 par 的 group 键取值;(b)「本 run 的 scope 标识」注入形态 | 225 行;276/329 行 |
| K5 | 无定义残留:#734 问题节"C1–C4"是旧树内部编号,新树无定义;未识别引用 #727、#744 | 367 行;192、294、347 行 |

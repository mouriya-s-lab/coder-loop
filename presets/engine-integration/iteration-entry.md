engine-integration deterministic stub protocol (no LLM; parsed line-wise by scripts/engine-integration-stub-runner.ts).

PHASE=iteration
CHAIN={{CHAIN_NAME}}
ITEM={{ITEM_KEY}}
RUN={{RUN_ID}}

Task: write the RUN value into `engine-integration-marker.txt` at the worktree root and commit it.

# Experiment contract

Select exactly one latest valid `<!-- moat-experiment:current -->` packet from the issue/run PR. It must bind hypothesis, environment identity, baseline, ordered commands, acceptance observations, passed/failed/inconclusive branches, evidence paths, cleanup obligations, IaC children, and owning design location. Missing, ambiguous, stale, or contradictory packets return to contract enrichment; downstream phases never silently repair them.

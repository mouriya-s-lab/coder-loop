# Orient

Read repository rules, lifecycle scripts, complete issue/PR timelines, linked IaC children, existing run directories, and live site state. Reuse an unfinished coherent run; otherwise create one run branch, draft PR, and run directory. Orientation is read-only with respect to the experiment site.

name = "warning"
[item]
idField = "id"
[statuses]
continuable = ["pending", "unused"]
terminal = ["done", "exhausted"]
entry = "pending"
exhausted = "exhausted"
[[phases]]
name = "run"
prompt = "run.md"
[[phases.exits]]
status = "done"
when = "done"

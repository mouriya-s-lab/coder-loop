engine-e2e deterministic stub protocol (no LLM; parsed line-wise by scripts/engine-e2e-stub-runner.ts).

PHASE=iteration
CHAIN={{CHAIN_NAME}}
ITEM={{ITEM_KEY}}
RUN={{RUN_ID}}

Task: write the RUN value into `engine-e2e-marker.txt` at the worktree root and commit it.

# Evidence

For every command preserve timestamp, cwd/host, exact command, exit status, relevant output, version/identity, and artifact path. Raw evidence is immutable; indexes and manifests cite it by checksum. Build, lint, mocks, summaries, and runner assertions do not replace the packet's real experiment path. Verify every referenced committed file exists and is non-empty.

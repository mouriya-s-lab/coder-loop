# Stop action

When restore cannot complete automatically or a required irreversible GitHub effect fails, publish the exact current state, residuals, safe operator commands, and re-entry condition. Invoke the declared stop chain action and verify the chain stopped. Never write a terminal success alongside stop.

# Export stage

Copy all raw outputs and relevant logs into the run tree, generate the evidence index and checksummed manifest, and verify provenance and non-empty files. Export transforms location/indexing only; it must not edit observations. Write the restore handoff with phase and path.

name = "warning"
[item]
idField = "id"
[routing]
continuable = ["pending", "unused"]
terminal = ["done", "exhausted"]
entry = "pending"
exhausted = "exhausted"
[[steps]]
name = "run"
prompt = "run.md"
[[steps.handoffs]]
status = "done"
when = "done"

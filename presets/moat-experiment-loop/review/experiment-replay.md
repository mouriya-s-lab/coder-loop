# Experiment replay

Replay deterministic manifest, provenance, restore, and acceptance checks that do not require a destroyed site. A missing live-site observation requires retry_prepare, never an invented offline substitute. Record commands and outcomes in the run PR.

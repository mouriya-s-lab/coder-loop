# State write

Query admitted exits for this run and phase, select by the declared `when` meaning, and write through the daemon CLI once. Verify JSON and then re-read live GitHub state. Preserve blocker metadata only for blocked. Never write an undeclared value or directly mutate SQLite/runtime files.

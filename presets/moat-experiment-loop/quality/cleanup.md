# Cleanup

Track every mutation in `run-state.md` with its inverse, owner, and proof. Happy and exceptional paths both pass restore before terminal review. Restore must first validate the PR handoff `phase` and `path`; do not run destructive cleanup when provenance is unclear. Completion requires target-owned restore plus no-residual checks on every affected host/service/dataset.

# Writeback stage

Derive findings from committed evidence, state passed/failed/inconclusive without embellishment, update `EXPERIMENTS.md`, and prepare the owning design diff. Cross-check every number and link. Leave both PRs reviewable but do not merge or close.

# Restore stage

Parse and validate the upstream handoff before cleanup. Run the repository-owned down/reset/restore path and the documented no-residual checks. Update cleanup obligations and evidence. On exceptional cleanup, route directly to review only after the site is proven clean; on happy cleanup, continue to writeback.

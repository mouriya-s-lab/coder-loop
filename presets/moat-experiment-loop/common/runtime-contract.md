# Runtime contract

Coder-loop schedules declared phases and admits only declared exits. It does not judge experiments, infer success from stdout, own GitHub truth, or clear a live site. Use runtime inputs as addresses, not evidence. Read current repository rules and current live state before every action. Runner output and session memory are not durable handoff. Never expose the run credential.

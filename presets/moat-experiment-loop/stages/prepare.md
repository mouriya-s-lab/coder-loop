# Prepare stage

Confirm the documented serial window, clean baseline, capacity, network, credentials from configured stores, run directory, and child prerequisites. Do not mutate the experiment deployment. Record preflight and baseline evidence plus a durable handoff.

# Experiment stage

Execute the declared workload against the identified deployment. Preserve raw output and observation timestamps. Decide only whether deployment remains a valid precondition and whether evidence can distinguish the acceptance branches; do not write findings.
